name: One-shot append Chapter 231
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/append-ch231-once.yml
permissions:
  contents: write
jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Append verified chapter
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          manuscript = Path('state/manuscript/Peg_Leg_Greg_Running_Manuscript.md')
          payload = Path('state/manuscript/.ch231_append.tmp')
          text = manuscript.read_text()
          chapter = payload.read_text()
          assert '# CHAPTER 230' in text and '## THE REPLICATE' in text, 'Chapter 230 authority missing'
          assert '# CHAPTER 231' not in text, 'Chapter 231 already exists; refusing competing append'
          assert '# CHAPTER 231' in chapter and '## THE MAGISTRATE' in chapter, 'Chapter 231 title/header mismatch'
          assert chapter.count('—') == 0, 'em dash in Chapter 231'
          words = len(re.findall(r"\b[\w’'-]+\b", chapter))
          assert 2500 <= words <= 4000, words
          manuscript.write_text(text.rstrip() + '\n\n' + chapter.lstrip())
          print('chapter_words', words)
          print('em_dashes', chapter.count('—'))
          PY
      - name: Remove one-shot files
        run: rm state/manuscript/.ch231_append.tmp .github/workflows/append-ch231-once.yml
      - name: Verify, commit, rebase, and push
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          text = Path('state/manuscript/Peg_Leg_Greg_Running_Manuscript.md').read_text()
          assert text.count('# CHAPTER 231') == 1
          tail = '# CHAPTER 231' + text.split('# CHAPTER 231', 1)[1]
          assert '## THE MAGISTRATE' in tail
          assert tail.count('—') == 0
          words = len(re.findall(r"\b[\w’'-]+\b", tail))
          assert 2500 <= words <= 4000, words
          print('verified_words', words)
          print('verified_em_dashes', tail.count('—'))
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add state/manuscript/Peg_Leg_Greg_Running_Manuscript.md state/manuscript/.ch231_append.tmp .github/workflows/append-ch231-once.yml
          git diff --cached --check
          git commit -m "Append Chapter 231: THE MAGISTRATE"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
