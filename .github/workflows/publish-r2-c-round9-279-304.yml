name: Publish Experiment C Round 09

on:
  push:
    branches:
      - publish/r2-written-c-round9-279-304
    paths:
      - r2/publish_round9.py

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch reconciled experiment authority
        run: git fetch origin experiment/r2-temporal-c-hybrid
      - name: Materialize selected prose and reader state
        run: python r2/publish_round9.py
      - name: Run R2 reader contract
        run: python -m unittest tests.test_r2_site -v
      - name: Commit publication transaction
        run: |
          rm -f r2/publish_round9.py .github/workflows/publish-r2-c-round9-279-304.yml
          git config user.name "experiment-c-publisher"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Publish Experiment C Round 09 Chapters 279-304" || exit 0
          git push origin HEAD:publish/r2-written-c-round9-279-304
